# Node.js API Configuration
API_PORT=8080
FRONTEND_URL=http://localhost:5174
LOG_LEVEL=info
BACKEND_URL=http://localhost:8080

# The path to the data directory, used by the Python worker
# For the Python worker in the `api` container, this path is not used directly,
# but it's good practice to have it defined.
DATA_DIR=/data
